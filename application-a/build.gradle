plugins {
    id('java')
}

dependencies {
    implementation(project(":application-b"))
}

jar {
    manifest {
        attributes 'Main-Class': 'com.example.a.Main'
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}